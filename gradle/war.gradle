apply plugin: "war"

bootWar {
    mainClass = "com.github.doudoudope.CourseEnrollBackendApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
